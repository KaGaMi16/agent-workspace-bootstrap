interface:
  display_name: "AI Infra Quickstart"
  short_description: "Safely build and consolidate local agent infrastructure"
  default_prompt: "Use $kgm-ai-infra-quickstart to inventory this machine and show the exact migration plan before changing files."
policy:
  allow_implicit_invocation: true

