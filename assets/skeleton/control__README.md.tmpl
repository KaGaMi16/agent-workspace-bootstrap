# Control

This directory contains local maintenance tools. The authoritative portable content is in `../content/`; private runtime state and migration receipts are in `../state/`.

These tools do not commit, push, publish, or delete backups.

