# Global instructions

This file loads into every Claude Code session on this machine (it is symlinked from `~/.claude/CLAUDE.md`). It starts intentionally minimal — fill it in with your own preferences as you notice yourself repeating the same corrections.

## Communication

- (Add how you want explanations pitched — technical depth, verbosity, language.)

## Workflow

- (Add how you want non-trivial tasks handled — planning before big changes, confirmation before risky operations, testing expectations.)

## Skill Auto-Trigger

Skills live in `../../skills/` (i.e. `kgm-agent-workspace/content/skills/`), symlinked to `~/.claude/skills`. Before starting a task, check whether an available skill matches the request; if so, read its `SKILL.md` and follow it.

---

Edit this file directly — changes take effect in your next session. There is no need to keep this comment once you've replaced the sections above.
