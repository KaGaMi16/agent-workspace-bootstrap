# hooks/

Claude Code hook scripts go here (symlinked from `~/.claude/hooks`). Hooks are shell commands the harness runs automatically on events like tool calls — see Claude Code's hooks documentation before adding one.

Starts empty. Nothing runs until you add a hook and register it in `../settings.json`.
