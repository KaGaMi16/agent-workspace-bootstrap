# Global instructions (Codex)

This file loads into every Codex CLI session on this machine (it is symlinked from `~/.codex/AGENTS.md`). It starts intentionally minimal — fill it in with your own preferences as you notice yourself repeating the same corrections.

## Workflow

- (Add how you want non-trivial tasks handled — planning before big changes, confirmation before risky operations, testing expectations.)

## Skill Auto-Trigger

Skills live in `../../skills/` (i.e. `ai-infra/content/skills/`), symlinked to `~/.codex/skills`. Before starting a task, check whether an available skill matches the request; if so, read its `SKILL.md` and follow it.
