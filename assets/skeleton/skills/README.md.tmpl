# Shared Skills

Put active Skill packages under this directory. Each active package contains `SKILL.md` with `name` and `description` frontmatter.

After changes, run:

```bash
python3 -B control/bin/rebuild-claude-md.py
bash control/bin/validate-skills.sh
```
